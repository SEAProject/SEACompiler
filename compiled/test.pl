use strict;
use warnings;
use stdlib::util;
use stdlib::array;
use stdlib::hashmap;
use stdlib::integer;
use stdlib::string;
use stdlib::boolean;


my $a1 = stdlib::integer->new(5);
my $a2 = stdlib::integer->new(10);
