use Lib::core qw(f1 f2);
use strict;
use warnings;
use stdlib::util;
use stdlib::array;
use stdlib::hashmap;
use stdlib::integer;
use stdlib::string;
use stdlib::boolean;


my $a = stdlib::integer->new(5);
